import { ScrollView, Text } from 'react-native';

const UserScreen = () => {
    return (
        <ScrollView>
            <Text>
                User
            </Text>
        </ScrollView>
    );
};

export default UserScreen;