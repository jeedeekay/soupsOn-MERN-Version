import { ScrollView, Text } from 'react-native';

const RecipeScreen = () => {
    return (
        <ScrollView>
            <Text>
                Recipes
            </Text>
        </ScrollView>
    );
};

export default RecipeScreen;