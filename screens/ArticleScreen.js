import { ScrollView, Text } from 'react-native';

const ArticleScreen = () => {
    return (
        <ScrollView>
            <Text>
                Articles
            </Text>
        </ScrollView>
    );
};

export default ArticleScreen;